{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Config",
  "type": "object",
  "required": [
    "asset_infos",
    "compounder",
    "farm_contract",
    "lp_token",
    "minimum_reward_to_compound",
    "owner",
    "pair_contract",
    "reward_to_lp_token_route",
    "reward_token",
    "slippage_tolerance"
  ],
  "properties": {
    "asset_infos": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/AssetInfo"
      },
      "maxItems": 2,
      "minItems": 2
    },
    "compounder": {
      "description": "The trusted address allowed to call the Compound function.",
      "allOf": [
        {
          "$ref": "#/definitions/Addr"
        }
      ]
    },
    "farm_contract": {
      "description": "The address of the farm/staking contract.",
      "allOf": [
        {
          "$ref": "#/definitions/Addr"
        }
      ]
    },
    "fee_percentage": {
      "description": "The percentage of rewards to take as a fee.",
      "anyOf": [
        {
          "$ref": "#/definitions/Decimal"
        },
        {
          "type": "null"
        }
      ]
    },
    "fee_recipient": {
      "description": "The address that will receive the compounding fees.",
      "anyOf": [
        {
          "$ref": "#/definitions/Addr"
        },
        {
          "type": "null"
        }
      ]
    },
    "lp_token": {
      "description": "The contract address of the LP token.",
      "allOf": [
        {
          "$ref": "#/definitions/AssetInfo"
        }
      ]
    },
    "minimum_reward_to_compound": {
      "$ref": "#/definitions/Uint128"
    },
    "owner": {
      "description": "The contract's owner, who can change configuration.",
      "allOf": [
        {
          "$ref": "#/definitions/Addr"
        }
      ]
    },
    "pair_contract": {
      "description": "The address of the AMM pair contract.",
      "allOf": [
        {
          "$ref": "#/definitions/Addr"
        }
      ]
    },
    "proposed_owner": {
      "description": "A new owner address that has been proposed but not yet accepted.",
      "anyOf": [
        {
          "$ref": "#/definitions/Addr"
        },
        {
          "type": "null"
        }
      ]
    },
    "reward_to_lp_token_route": {
      "description": "A list of swap operations to perform to convert the reward token into one of the two underlying assets of the `pair_contract`. If this route is empty, the contract assumes the reward token is already one of the LP's assets.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/SwapHop"
      }
    },
    "reward_token": {
      "description": "The address of the farm's reward token.",
      "allOf": [
        {
          "$ref": "#/definitions/AssetInfo"
        }
      ]
    },
    "slippage_tolerance": {
      "description": "The slippage tolerance to use for swaps during compounding.",
      "allOf": [
        {
          "$ref": "#/definitions/Decimal"
        }
      ]
    }
  },
  "additionalProperties": false,
  "definitions": {
    "Addr": {
      "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
      "type": "string"
    },
    "AssetInfo": {
      "description": "AssetInfo contract_addr is usually passed from the cw20 hook so we can trust the contract_addr is properly validated.",
      "oneOf": [
        {
          "type": "object",
          "required": [
            "token"
          ],
          "properties": {
            "token": {
              "type": "object",
              "required": [
                "contract_addr"
              ],
              "properties": {
                "contract_addr": {
                  "type": "string"
                }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": [
            "native_token"
          ],
          "properties": {
            "native_token": {
              "type": "object",
              "required": [
                "denom"
              ],
              "properties": {
                "denom": {
                  "type": "string"
                }
              },
              "additionalProperties": false
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "Decimal": {
      "description": "A fixed-point decimal value with 18 fractional digits, i.e. Decimal(1_000_000_000_000_000_000) == 1.0\n\nThe greatest possible value that can be represented is 340282366920938463463.374607431768211455 (which is (2^128 - 1) / 10^18)",
      "type": "string"
    },
    "SwapHop": {
      "type": "object",
      "required": [
        "pair_contract",
        "to_asset_info"
      ],
      "properties": {
        "pair_contract": {
          "description": "The address of the pair contract for this specific hop.",
          "allOf": [
            {
              "$ref": "#/definitions/Addr"
            }
          ]
        },
        "to_asset_info": {
          "description": "The asset we expect to receive from this swap.",
          "allOf": [
            {
              "$ref": "#/definitions/AssetInfo"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "Uint128": {
      "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
      "type": "string"
    }
  }
}
